

server:
  port: 8081

logging:
  level:
    anh.pham.iss: info

spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#      generate-ddl: true
    show-sql: false
#    hibernate:
#      ddl-auto: create
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/iss
    username: postgres
    password: postgres
#
#    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=- 1;DB_CLOSE_ON_EXIT=FALSE
#    username: test
#    password: test
#
